module.exports = {
objectsDeeplyEqual:function(cmp1, cmp2) {
  if (cmp1 === cmp2){

  }

  var cmp1k = cmp2.key ;
  var cmp2k = cmp2.key ;
  let i = 0;

  while (i<cmp1k.length) {
    if (cmp1k[i] === cpm2k[i]) {
        if (cmp1.cmp1k[i] === cmp2.cmp2k[i]) {

        }
        else {
          return console.log(false));
        }
    }
    else {
      return console.log(false));
    }
  }

  }
}
